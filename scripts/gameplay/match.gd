extends Node3D
## Match scene: currently the visual/atmosphere testbed. Procedural building,
## environment and first-person controller grow here iteration by iteration.

func _ready() -> void:
	print("MATCH: scene ready")